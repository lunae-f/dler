services:
  dler-server:
    restart: unless-stopped
    build:
      context: .
      dockerfile: httpd/Dockerfile
    volumes:
      - ./downloaded:/usr/local/apache2/htdocs/downloaded:ro
    ports:
      - 8080:80